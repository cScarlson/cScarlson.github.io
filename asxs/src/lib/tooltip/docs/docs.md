
## Tooltips
`* > as-tooltip[position="(block | inline)-(start | end)"][style="--delay: {delay}"]`

### Setup

${setup}

```html
${ escape(setup) }
```

### Positioning & Delay

${example}

```html
${ escape(example) }
```
