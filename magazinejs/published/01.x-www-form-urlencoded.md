
x-www-form-urlencoded
================

Here's all you need...

```typescript
const data = new URLSearchParams({ q: 'some search query', more: 'another value' });
const urlEncoded = data.toString();  // or `${data}` or '' + data;
```
